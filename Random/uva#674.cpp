#include<bits/stdc++.h>
using namespace std;
#define sz 100005
int a[sz];
int main()
{
    int n;
    int b[]={0,1,5,10,25,50};
    ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
    while(cin>>n)
    {
        memset(a,0,sizeof(a));
        if(n!=0)
            s++,for(int i=1;i<=n;i++),a[i]=1;
        while(1)
        {
            int x=n;
            while(x)
            {

            }
        }
    }
    return 0;
}
